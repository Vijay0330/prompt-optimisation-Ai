
# ── Force pip to use pre-built wheels only, never compile from source.
# Add this to your Render build command instead:
#   pip install --only-binary=:all: -r requirements.txt || pip install -r requirements.txt
#
# Render build command should be:
#   pip install -r requirements.txt
#
# Python runtime: set to 3.11 in render.yaml (has wheels for everything below)

fastapi==0.115.0
uvicorn[standard]==0.31.1
httpx==0.27.2

# Pin pydantic AND pydantic-core explicitly — prevents pip from upgrading
# pydantic-core to a version that requires Rust compilation on Python 3.14
pydantic==2.10.6
pydantic-core==2.27.2
email-validator==2.2.0

python-dotenv==1.0.1
motor==3.6.0
pymongo==4.9.2
python-jose[cryptography]==3.3.0

passlib[bcrypt]==1.7.4
bcrypt==4.0.1

fpdf2==2.7.9
python-pptx==1.0.2
markdown2==2.5.0

# ── Phase 5: RAG ─────────────────────────────────────────────────────────────
# chromadb 0.6.3 pulls hnswlib which needs C++ compiler
# Use 0.5.3 which ships pre-built wheels for all platforms
chromadb==0.5.3
sentence-transformers==2.7.0
pypdf==5.1.0
python-multipart==0.0.20
duckduckgo-search==6.3.7
